console.log("lets learn about the math  obj")
let x = 3;
let y = 6;
let z;
//normal maths objs ar +,-,/,*,;


z=Math;
//this will show the all maths object in console
z=Math.PI//print the value of the pi
z=Math.E;// print the value of E 
z=Math.round(6.5)// print the round figure of the enterd numebr 
z=Math.ceil(5.3); // this will round enterd to the value to the higher numeber
z=Math.floor(6.8) // this will round the value to the lower number
z=Math.abs(-5.2) // this will print the negative numeber to the positive and postive as it is
z=Math.sqrt(49)// print the square root of the enterd number

z=Math.pow(2,3) // its means 2^3
z=Math.min(3,5,6,7,8)// this will print the min of the enterd number
z=Math.max(3,5,6,7,8)// this will print the max of the enterd number
z=Math.random() // this wll print the random number
//but this random no is in range of the 0 to 1 so if u want to convert into the range of the 0 to 100 the do this
z=100*Math.random()
//if want to the print random no of the range of the x to 100 then do like this lets take EX:50 to 100
z= 50+(100-50)*Math.random()
console.log(z)